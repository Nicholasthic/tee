# Courses Scott and Nick have played.
#
# This is the shared record — it's what both of you see when the page is
# rebuilt. Ticking on the page saves to that device only; use the "Copy for
# played.yaml" button there, paste the list below, and commit it so the tick
# shows for the other person too.
#
# Names must match `name` in clubs.yaml exactly; the build warns if one drifts.

played: []
